export default class Sprite {
  constructor(url, pos, size) {
    this.url = url;
    this.pos = pos;
    this.size = size;
    // this.dir = dir;
    // this.speed= speed;
    // this.frames = frames; 
    // this._index = 0;
  }

};